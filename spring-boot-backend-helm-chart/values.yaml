replicaCount: 1

namespace: spring-app

releaseName: backend

image:
  repository: "yadnesh082024/github-actions-backend"
  pullPolicy: IfNotPresent
  tag: main-20240930150921

service:
  type: NodePort
  port: 80
  nodePort: 30003

containers:
  port: 8080

podLabels: {}  # Add this if needed to customize pod labels

ingress:
  enabled: true
  paths:
    - path: /
      pathType: Prefix




