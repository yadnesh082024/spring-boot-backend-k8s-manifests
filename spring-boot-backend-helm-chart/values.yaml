replicaCount: 1

namespace: spring-app

image:
  repository: "yadnesh082024/github-actions-backend"
  pullPolicy: IfNotPresent
  tag: main-20240925111805

service:
  type: NodePort
  port: 80
  nodePort: 30003

containers:
  port: 8080

ingress:
  enabled: true
  host: spring-app.25c0ba1c405d4f52be1a.eastasia.aksapp.io
  annotations:
    kubernetes.io/ingress.class: addon-http-application-routing
  paths:
    - path: /
      pathType: Prefix
      backend:
        service:
          name: spring-app
          port:
            number: 80
