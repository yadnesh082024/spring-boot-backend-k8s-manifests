replicaCount: 1

namespace: spring-app

image:
  repository: "yadnesh082024/github-actions-backend"
  pullPolicy: IfNotPresent
  tag: main-20240925111805

service:
  type: NodePort
  port: 80
  nodePort: 30003

containers:
  port: 8080

ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: nginx
  paths:
    - path: /
      pathType: Prefix
